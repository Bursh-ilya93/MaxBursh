export const sendComment = (data) => dispatch => {
	console.log('типо отправляю коммент');

	// Xhr.login().then((resp) => {
	// 	dispatch({
	// 		type : ACTIONS.USER.LOGIN,
	// 		data : resp.data,
	// 	});
	//
	// });
};
